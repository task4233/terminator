<% if user_signed_in? %>
  <div class="row center">
    <div class="col-md-10 col-md-offset-1">
      <h2>お疲れ様です, <%= current_user.name %>さん!</h2>
      <section class="task_form">
        <%= render 'tasks/task_form' %>
      </section>
    </div>
  </div>
  <div class="collection center">
    <% if @user.tasks.count == 0 %>
    <h3>No Todos!</h3>
    <p>現在, 未完了のTodoはありません。<br>
      Todoを追加したい場合は, 上のフォームから追加してください。
    </p>
    <% else %>
    <h3>Tasks(<%= @user.tasks.count %>)</h3>
    <% if @user.tasks.count > 3 %>
      <%= link_to '全てのTodoを確認する', users_path %>
    <% end  %>
    <table class="tasks-columns">
      <tbody>
        <%= render @user.tasks.all.order(updated_at: "DESC").take(3)  %>
      </tbody>
    </table>
    <% end %>
  </div>  
<% else %>
<div class="center jumbotron">
  <h1>Welcome!</h1>
  <div class="btn btn-lg">
    <%= render "devise/shared/links" %>
  </div>
</div>
<div class="container">
  <h2>Q. このアプリケーションで何ができるの?</h2>
  <h2 class="ans">A. Todoを管理することができます。</h2>

  <h2>Q. 既存のTodoアプリと何が違うの?</h2>
  <h2 class="ans">A. 以下のような違いがあります。</h2>
  <div class="col-md-8 col-md-offset-1">
    <table>
      <thead>
        <tr>
          <th>比較ポイント</th>
          <th>TodoアプリA</th>
          <th>TodoアプリB</th>
          <th>Terminator</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th>ホーム画面で表示されるTodoの数</th>
          <th>7-8個</th>
          <th>10-11個</th>
          <th>3個</th>
        </tr>
      </tbody>

    </table>
  </div>

  <h1></h1>
</div>
<% end %>
